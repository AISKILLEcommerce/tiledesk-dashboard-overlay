{
  "name": "tiledesk-dashboard-overlay",
  "version": "0.1.0",
  "description": "Overlay kit for self-hosting Tiledesk Dashboard with config audit helpers and deployment templates.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check adapters/audit-config.js && node --check adapters/audit-lib.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
